const ticTacToe = () => {
    const header = () => {} // creates the header element. creates a nav element. creates a page title.
    const main = () => {} // creates a main element. creates a section element, creates an aside element. creates a start button.
    const footer = () => {} // creates the footer. creates footer text
}